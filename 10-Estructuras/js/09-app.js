const autenticado = false;
const puedePagar = false;

console.log( autenticado ? puedePagar ? 'Si esta autenticado y puede pagar' : 'Si esta auntenticado, no puede pagar' : 'No esta autenticado' );