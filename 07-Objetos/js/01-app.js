const nombre = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

//Un objeto agrupa a todas las variables en una sola

//Object Literal
const producto = {
    nombre: "Monitor 20 pulgas",
    precio: 300,
    disponible: true
}

console.log(producto);